<menu xmlns:android="http://schemas.android.com/apk/res/android" xmlns:gsantiago1="http://schemas.android.com/apk/res-auto" >
    <!-- Search, should appear as action button -->
      <item android:id="@+id/actualizar"
        android:icon="@drawable/ic_action_refresh"
        android:title="@string/action_search"
        gsantiago1:showAsAction="always" />
    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/registrar"
        android:title="Registrar"
        gsantiago1:showAsAction="never" />
    <item android:id="@+id/unregistrar"
        android:title="Cancelar Registro"
        gsantiago1:showAsAction="never" />

    <item android:id="@+id/preferencias"
        android:title="Preferencias"
        gsantiago1:showAsAction="never" />
</menu>
